# CsvForge Samples

- `CsvForge.GeneratedSerializerSample` is retained as a **legacy** sample for backward compatibility.
- Prefer the newer scenario-oriented samples (`Basic`, `Advanced`, `Checkpointing`, `Compression`, `Excel`, `NativeAot`) for current guidance.
